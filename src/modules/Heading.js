import { Typography } from "@mui/material";

const Heading = props => {
    return(
        <Typography elevation={0} variant='h3' sx={{pt:15, pb:4}}>Exam</Typography>
    )
};

export default Heading;